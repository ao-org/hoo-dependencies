# HOO build dependencies

Immutable mirrors of redistributable third-party binary dependencies used by Heroes of Old builds.

Each release records its upstream source, exact version, archive size, license variant, and SHA-256 digest. Dependency consumers must pin and verify the digest.
